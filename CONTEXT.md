# Jenkins Shared Library Delivery

This context describes the language used for delivering applications through the Jenkins Shared Library.

## Language

**应用交付（Application Delivery）**:
由构建、产物存储和部署组成的完整过程。
_Avoid_: 发布流程、部署流程

**产物准备（Artifact Preparation）**:
执行构建以及可选的产物存储，但不执行部署的过程。产物准备成功不表示应用已进入目标环境。
_Avoid_: 应用交付、部署成功

**交付预检（Delivery Preflight）**:
在执行任何构建、产物存储或部署阶段前，对有效交付配置及其阶段组合、必填字段、策略约束、产物依赖、应用标识、源码引用和目标节点进行的完整校验。预检将源码引用解析并固定为本轮已解析源码版本；预检失败时不产生交付副作用。
_Avoid_: 阶段内延迟校验、就绪验证

**分发预检运行（Dispatch Dry Run）**:
仅由仓库分发入口提供的运行方式；仓库分发 Job 扫描和解析全部候选仓库并执行完整交付预检，但不执行构建、产物存储、部署或交付通知。直接交付入口不对外提供独立预检模式。
_Avoid_: 部分解析检查、真实应用交付

**分发顺序（Dispatch Order）**:
仓库分发 Job 先按源码仓库名称、再按配置文件相对路径排序后，串行执行有效可交付单元的确定性顺序。该顺序不表达应用依赖关系。
_Avoid_: API 返回顺序、依赖编排

**构建（Build）**:
从应用源代码生成构建产物的交付阶段。
_Avoid_: 打包、编译

**构建产物（Build Artifact）**:
构建阶段生成并可供后续部署使用的不可执行或可执行交付物。
_Avoid_: 构建包、发布包

**产物存储（Artifact Storage）**:
保存本次执行产物并使其可被后续部署获取的交付阶段。当前启用产物存储时必须同时启用构建阶段，关闭构建而启用存储属于无效配置。
_Avoid_: 存包、归档、历史产物复制

**部署（Deployment）**:
将本次执行产物置入目标运行环境的交付阶段。当前部署必须与构建阶段组合，关闭构建而启用部署属于无效配置。
_Avoid_: 发布、仅重启、历史产物部署

**交付阶段（Delivery Stage）**:
应用交付中可通过配置启用或停用的组成部分，包括构建、产物存储和部署。阶段组合仍需满足所选构建、存储和部署策略的依赖约束，且至少启用构建阶段；三个阶段全部关闭属于无效配置。
_Avoid_: 发布步骤

**空交付配置（Empty Delivery Configuration）**:
构建、产物存储和部署三个交付阶段全部关闭的无效配置。它必须在交付预检时失败，不能作为成功的空操作；只校验需求应使用分发预检运行。
_Avoid_: 分发预检运行、成功交付、成功产物准备

**产物引用（Artifact Reference）**:
对一份可获取构建产物的稳定标识。当前正式能力只支持引用本次环境交付执行生成并存储的产物。
_Avoid_: 包地址、文件路径

**存储产物身份（Stored Artifact Identity）**:
构建产物进入外部产物存储后使用的不可变唯一身份，至少由目标环境、应用标识和 Jenkins 执行标识区分，并关联内容校验值。不同单元的源文件名可以相同，但不得解析到同一存储身份；已存在的身份不能被覆盖。
_Avoid_: 源文件名、工作区路径、可覆盖存储地址

**本次执行产物（Current-execution Artifact）**:
由当前环境交付执行的构建阶段生成，供同一次执行的产物存储或部署阶段消费的构建产物。当前启用部署阶段时必须同时启用构建阶段，以提供本次执行产物。
_Avoid_: 历史产物

**直接产物交接（Direct Artifact Handoff）**:
不启用产物存储阶段时，由部署策略在同一次环境交付执行中直接获取并消费构建产物的方式。只有部署策略能够保证产物对部署位置可达时才是合法阶段组合。
_Avoid_: 产物存储、历史产物部署、不可达构建产物

**历史产物部署（Historical Artifact Deployment）**:
跳过当前构建并通过产物引用部署过去执行所生成产物的规划能力。当前共享库尚未正式支持。
_Avoid_: 本次执行产物、当前部署能力

**已部署应用实例（Deployed Application Instance）**:
应用被部署到目标运行环境后形成的可管理实例。
_Avoid_: 发布结果

**应用运维操作（Application Operation）**:
不产生或部署新构建产物，而是改变已部署应用实例状态或存在性的规划能力，包括服务管理和销毁。当前共享库尚未正式支持应用运维操作。
_Avoid_: 应用交付、发布

**服务管理（Service Management）**:
规划中的启动、停止或重新启动已部署应用实例的应用运维操作。
_Avoid_: 服务发布

**销毁（Teardown）**:
规划中的移除已部署应用实例的应用运维操作。
_Avoid_: 下线发布

**规划能力（Planned Capability）**:
已经进入领域讨论但尚未由正式共享库入口支持的能力。规划能力不能在当前配置或文档中被描述为可用功能。
_Avoid_: 已支持能力、弃用能力

**不支持的操作模式（Unsupported Operation Mode）**:
当前入口尚未实现或无法识别的应用运维模式。配置显式请求不支持的模式时必须被拒绝，不能静默回退为应用交付。
_Avoid_: 默认交付、规划能力已实现

**默认操作模式（Default Operation Mode）**:
旧配置未声明操作模式时采用的 `DEPLOY` 行为。只有缺省值触发默认交付，显式填写的不支持模式必须失败。
_Avoid_: 未知模式回退

**目标环境（Target Environment）**:
应用交付所面向的独立运行环境，例如测试环境或生产环境。
_Avoid_: 部署节点、Jenkins Job

**环境交付 Job（Environment Delivery Job）**:
绑定且只绑定一个目标环境，并独立执行该环境应用交付的 Jenkins Job。它既可以直接交付一个应用，也可以作为仓库分发 Job 交付多个应用；不同目标环境的 Job 不共享或晋级构建产物。
_Avoid_: 环境、部署节点、跨环境发布

**环境交付配置（Environment Delivery Configuration）**:
描述应用在一个目标环境中如何完成交付的配置。它可以保存在源码仓库中，也可以直接定义在对应的环境交付 Job 中。
_Avoid_: 跨环境配置、共享库默认配置

**Job 内联配置（Job-inline Configuration）**:
直接定义在环境交付 Job 中，而不是从源码仓库读取的环境交付配置。
_Avoid_: 仓库交付配置

**主交付配置（Primary Delivery Configuration）**:
一次应用交付所使用的唯一主要配置。直接交付 Job 使用 Job 内联配置，仓库分发 Job 使用目标仓库中的仓库交付配置。
_Avoid_: 有效交付配置、共享库默认配置

**有效交付配置（Effective Delivery Configuration）**:
共享库默认配置、调用方默认覆盖、扩展配置、主交付配置和执行覆盖参数完成合并后，实际支配本次应用交付的配置。
_Avoid_: 原始配置、主交付配置

**共享库默认配置（Shared Library Default Configuration）**:
由共享库内置默认配置文件提供、由当前统一交付入口自动加载并为应用交付补充基础值的最低优先级配置。
_Avoid_: 主交付配置

**调用方默认覆盖（Caller Default Override）**:
由统一交付入口调用方提供、用于深度覆盖共享库默认配置的可选配置层。它的优先级高于共享库默认配置、低于扩展配置。
_Avoid_: 共享库默认配置、执行覆盖参数

**默认值清除标记（Default-clearing Marker）**:
凭据别名字段使用的精确字符串 `-`，表示清除从较低优先级配置层继承的凭据别名。凭据别名字段缺失或为空字符串时继续继承，`null` 是无效配置值；其他配置字段不赋予 `-` 特殊语义。
_Avoid_: 空字符串、缺失字段、null

**扩展配置（Extension Configuration）**:
在共享库默认配置和调用方默认覆盖之上复用团队、组织或环境公共值的配置。
_Avoid_: 主交付配置、Job 参数

**执行覆盖参数（Execution Override）**:
由环境交付 Job 为本次执行提供、优先于其他配置层的最终覆盖值。
_Avoid_: 默认配置、应用配置

**共享库步骤（Shared Library Step）**:
供 Jenkins Pipeline 调用、聚焦一项主要交付或仓库编排职责的共享库能力。每种应用类型由对应的共享库步骤组织其应用交付。
_Avoid_: vars 文件、通用脚本

**Java 应用（Java Application）**:
以 JAR 或 WAR 作为主要构建产物，并由 Java 应用交付步骤处理的应用。
_Avoid_: Java 部署方式

**Web 应用（Web Application）**:
由 Web 应用交付步骤处理，并可通过静态目录或容器等方式部署的应用。
_Avoid_: 静态部署、Web 服务

**部署策略（Deployment Strategy）**:
部署阶段将构建产物置入目标运行环境的一种可选方式，例如静态目录部署或容器部署。一次部署只选择一种部署策略，且部署策略不决定应用类型。
_Avoid_: 应用类型、共享库步骤

**构建策略（Build Strategy）**:
构建阶段从源代码生成构建产物的一种可选方式。一次构建只选择一种构建策略。
_Avoid_: 应用类型、构建工具

**产物存储目标（Artifact Storage Target）**:
构建产物在产物存储阶段写入的一个可获取位置。当前一次交付最多选择一个产物存储目标；显式选择多个目标属于无效配置，必须在交付预检时失败。
_Avoid_: 部署目标、产物引用

**多目标产物存储（Multi-target Artifact Storage）**:
将同一构建产物写入多个产物存储目标的规划能力。当前共享库尚未正式支持。
_Avoid_: 当前产物存储能力、多个存储策略已启用

**就绪验证（Readiness Verification）**:
部署后用于判断已部署应用实例是否可用的一组有序验证。一次部署可以组合多个验证方式，前一项失败时不再继续后续验证。
_Avoid_: 构建测试、存活监控

**部署节点（Deployment Node）**:
目标环境中承载一个已部署应用实例的 Jenkins 节点。
_Avoid_: 目标环境、Node Label

**串行节点部署（Serial Node Deployment）**:
按顺序逐个部署并验证目标节点的多节点部署方式。任一节点失败后停止，已成功的节点保留新版本，失败节点和未开始节点不自动变更。
_Avoid_: 并行部署、自动回滚

**部分部署（Partial Deployment）**:
串行节点部署失败后，目标节点分别处于新版本、失败状态或旧版本的暂时不一致状态。
_Avoid_: 部署成功、完整回滚

**部署失败恢复（Deployment Failure Recovery）**:
部署策略在当前节点执行失败时，为恢复部署前状态而采取的局部保护措施。它不代表正式回滚，也不改变其他节点的版本。
_Avoid_: 回滚、重新交付

**交付结果（Delivery Outcome）**:
由包含部署阶段的本次交付中所有已启用交付阶段、就绪验证及工作区清理的执行结果共同确定的最终状态。未启用部署阶段的执行产生的是产物准备结果，而不是交付结果；交付通知不影响结果。
_Avoid_: 通知结果

**交付通知（Delivery Notification）**:
向协作平台发送应用交付状态的附属动作。通知失败只产生告警，不改变交付结果。
_Avoid_: 交付阶段、交付成功条件

**工作区清理（Workspace Cleanup）**:
应用交付结束时清除 Jenkins 工作区残留的强制收尾动作。清理失败会使交付结果失败，以防残留内容影响后续构建。
_Avoid_: 可选清理、交付通知

**工作区初始化（Workspace Initialization）**:
每个可交付单元开始前将 Jenkins 工作区恢复为空目录的强制动作。它先删除前序任务的全部工作区内容，再允许源码检出或任何交付阶段开始；初始化失败会使该单元立即失败。允许保留的声明式构建缓存必须位于工作空间之外。
_Avoid_: 完整工作区复用、可选清理

**声明式构建缓存（Declared Build Cache）**:
由交付配置明确允许跨执行和跨可交付单元共享、用于加速构建的数据。它位于工作空间之外，不能包含源码、构建产物或任务临时文件；工作空间本身始终完整清理并保持单元隔离。
_Avoid_: 工作区残留、构建产物、源码副本

**可交付单元执行隔离（Deliverable Unit Execution Isolation）**:
仓库分发 Job 串行执行可交付单元，并为每个单元建立独立的任务生命周期：开始前先执行工作区初始化，再独立检出源码和执行交付阶段，结束时执行工作区清理。不同单元可以顺序复用同一物理目录，但不得继承前一单元的构建产物、临时文件或清理状态；只有声明式构建缓存可以跨单元共享。初始化失败时当前单元立即失败，仓库分发 Job 继续尝试后续单元。
_Avoid_: 并发共享工作区、隐式文件复用、按单元长期保留目录

**托管凭据（Managed Credential）**:
由 Jenkins Credentials 保存并在交付执行时提供的真实密码、Token、私钥或其他敏感值。
_Avoid_: 仓库配置值、明文凭据

**凭据别名（Credential Alias）**:
交付配置用于引用托管凭据的非敏感稳定标识。仓库配置和 Job 内联配置只保存凭据别名，不保存真实凭据。
_Avoid_: 密码、Token、私钥

**源码凭据别名（Source Credential Alias）**:
有效交付配置为本轮源码访问解析出的唯一 Jenkins 凭据别名。显式配置优先；未显式配置的 SSH 仓库继承共享库 Git 凭据别名，HTTP/HTTPS 仓库不自动继承 SSH 凭据；当前 HTTPS 私有仓库认证尚未正式支持。
_Avoid_: 构建器凭据、硬编码 SSH ID、HTTPS Token 已支持

**交付声明（Delivery Opt-in）**:
可交付单元通过提供仓库交付配置，声明希望由仓库分发 Job 执行应用交付。交付声明本身不授予目标环境的交付权限。
_Avoid_: 环境授权、部署许可

**环境交付授权（Environment Delivery Authorization）**:
仓库分发 Job 授予某个源码仓库向其目标环境执行应用交付的权限。授权可以通过仓库白名单提供，或在整个代码托管空间均可信时显式授予全部仓库；获授权仓库在受控配置发现引用上的所有匹配可交付单元继承该授权，不维护单元级授权白名单。
_Avoid_: 仓库交付配置、交付声明、单元级白名单

**远端命令（Remote Command）**:
由仓库交付配置提供、将在环境交付 Job 的执行上下文中运行的构建或部署命令。
_Avoid_: 共享库内部命令、Job 管理命令

**已批准命令（Approved Command）**:
由环境交付 Job 精确授权、允许仓库交付配置选择的远端命令值。生产环境的仓库配置只能使用已批准命令。
_Avoid_: 任意 Shell、不安全命令字段

**被拒绝交付（Rejected Delivery）**:
仓库交付配置因不满足解析、方法白名单、命令授权或其他安全规则而未被执行的分发结果。
_Avoid_: 执行失败、主动跳过

**仓库分发结果（Repository Dispatch Outcome）**:
仓库分发 Job 处理全部候选可交付单元后的汇总结果。单个单元被拒绝或执行失败不会中断其他单元，但会使最终分发结果失败；已经成功的应用交付不回滚。
_Avoid_: 单元交付结果、遇错即停

**跳过仓库（Skipped Repository）**:
未提供当前目标环境所需仓库交付配置，因此未声明参与本次分发的源码仓库。跳过仓库不是被拒绝或失败，不影响仓库分发结果。
_Avoid_: 被拒绝交付、执行失败

**已撤回单元（Withdrawn Deliverable Unit）**:
递归扫描时被发现、但读取其最新配置内容前已确认配置文件被删除的候选可交付单元。该单元不执行且不使仓库分发结果失败，但必须在分发明细和汇总中留痕；无法确认文件已删除的读取异常不属于撤回。
_Avoid_: 被拒绝交付、读取失败、跳过仓库

**源码仓库（Source Repository）**:
保存一个或多个可交付单元源代码及其仓库交付配置的代码仓库。
_Avoid_: 代码托管空间

**应用（Application）**:
通过一个可交付单元维护，并由环境交付 Job 交付到目标环境的独立软件单元。
_Avoid_: 源码仓库、Jenkins Job、部署节点

**可交付单元（Deliverable Unit）**:
源码仓库中可独立配置和交付的应用单元。一份匹配的仓库交付配置代表一个可交付单元，一个源码仓库可以包含多个可交付单元。
_Avoid_: 源码仓库、目标环境

**可交付单元标识（Deliverable Unit Identifier）**:
由源码仓库稳定标识和仓库交付配置相对路径共同构成的配置来源身份，用于仓库分发明细和交付追溯，不用于分配长期独占工作目录。它不同于目标环境中的应用标识；配置文件路径变化会形成新的可交付单元标识。
_Avoid_: 应用标识、应用名称、仅配置文件名

**可交付单元源码目录（Deliverable Unit Source Root）**:
一个可交付单元执行构建时所使用的源码范围。默认是其仓库交付配置文件所在的模块相对目录，而不是源码仓库根目录；需要使用父级或其他源码范围时可以由该单元显式指定。该目录必须在所选源码引用中精确存在，否则预检失败，不回退或猜测其他目录。不同可交付单元的源码目录允许部分或完全重叠，单元边界由各自配置和应用标识确定。
_Avoid_: 固定仓库根目录、配置搜索起点、单元唯一标识

**单元相对路径（Unit-relative Path）**:
可交付单元配置中相对于该单元源码目录解析的路径，包括构建描述文件、容器构建文件和构建产物路径。规范化后的路径必须位于该单元源码目录内；需要访问父级工程时应扩大可交付单元源码目录，不能通过路径越界访问。
_Avoid_: 仓库根相对路径、工作区绝对路径、父目录穿越

**应用标识（Application Identifier）**:
在一个目标环境内唯一标识应用的稳定名称。直接交付 Job 中显式配置优先，未配置时默认使用 Jenkins Job 名；仓库分发中的每个可交付单元必须显式提供应用标识，缺失时预检失败。
_Avoid_: Jenkins 构建编号、部署节点名称

**缺失应用标识（Missing Application Identifier）**:
仓库分发中的可交付单元未显式提供应用标识的无效状态。缺失应用标识会使该单元预检失败，不允许回退为仓库名、目录名或 Jenkins Job 名。
_Avoid_: 默认应用标识

**应用标识冲突（Application Identifier Conflict）**:
同一目标环境中的多个可交付单元解析出相同应用标识的无效状态。仓库分发 Job 在执行交付前检查冲突，并拒绝所有冲突单元。
_Avoid_: 应用别名、合法覆盖

**配置发现引用（Configuration Discovery Reference）**:
由仓库分发 Job 指定、用于递归发现目标环境仓库交付配置的受控 Git 引用，通常指向默认分支或受保护分支。可交付单元不能通过自身配置改变配置发现引用；仓库分发期间每次读取都跟随该引用当时的最新状态，不冻结仓库级配置快照。
_Avoid_: 源码引用、任意业务分支

**已解析配置版本（Resolved Configuration Revision）**:
读取某一份仓库交付配置内容时，配置发现引用实际指向的 Commit SHA。它与仓库交付配置的相对路径共同唯一标识该单元本次使用的配置内容；同一次仓库分发中的不同配置可以来自不同 Commit。
_Avoid_: 已解析源码版本、配置发现引用

**本轮单元配置（Run-bound Unit Configuration）**:
仓库分发 Job 为某个可交付单元读取到的最新配置内容及其已解析配置版本。该内容通过交付预检后固定用于本轮执行，不在构建前再次读取；下一轮分发重新读取当时的最新配置。
_Avoid_: 仓库级配置快照、执行前动态配置

**源码引用（Source Reference）**:
交付配置用于选择实际参与构建的应用源码 Git 分支、Tag 或 Commit SHA。测试和生产环境均可使用任一种引用类型；源码引用不决定从哪里发现仓库交付配置。
_Avoid_: 配置发现引用、已解析源码版本

**已解析源码版本（Resolved Source Revision）**:
源码引用在交付预检时解析并固定供本轮构建使用的 Commit SHA。实际构建必须检出该版本，每次应用交付都必须记录该值，以便准确追溯所用源码。
_Avoid_: 分支名、Tag 名

**源码工作副本（Source Working Copy）**:
当前统一交付入口将已解析源码版本检出后形成、供本轮所有构建步骤共同消费的唯一工作区源码副本。构建策略不得再次检出另一份源码。
_Avoid_: 构建器私有检出、重复源码副本

**交付记录（Delivery Record）**:
由 Jenkins Build Record 保存的一次环境交付执行事实，至少包含应用、目标环境、配置来源、源码引用、已解析源码版本、产物、部署节点和交付结果。仓库分发明细额外记录可交付单元标识、配置文件路径和已解析配置版本；直接交付执行由 Jenkins Job 全名和 Build Number 标识。Jenkins 是当前交付记录的唯一事实来源。
_Avoid_: 通知消息、源码仓库提交记录

**仓库分发明细（Repository Dispatch Entry）**:
仓库分发记录中针对一个候选可交付单元保存的独立处理结果，包括被拒绝、执行失败或交付结果；没有匹配单元的源码仓库单独记录为跳过仓库。
_Avoid_: 仓库分发结果、汇总计数

**入口契约（Entry Contract）**:
一个共享库步骤对 Jenkins Pipeline 暴露的稳定调用方式及配置语义。入口契约必须长期兼容该入口曾经接受的旧配置。
_Avoid_: 配置版本、内部实现

**旧配置（Legacy Configuration）**:
某个共享库步骤在既有入口契约下曾经接受的历史配置结构。旧配置在入口被转换为当前统一结构后继续受支持。
_Avoid_: 无效配置、待删除配置

**替代入口（Replacement Entry）**:
当能力发生无法在原入口契约内兼容的重大变化时新增的共享库步骤。替代入口拥有独立契约，不通过原入口中的配置版本分支实现。
_Avoid_: configVersion、原入口破坏性升级

**弃用入口（Deprecated Entry）**:
已有替代入口且不再增加新能力，但仍保持原有行为和旧配置兼容的共享库步骤。只有完成使用方核对、迁移和明确公告后，才能通过独立决策删除。
_Avoid_: 已删除入口、不受支持入口

**取代通知（Supersession Notification）**:
环境交付执行被新执行自动取代时发送的交付通知，包含旧执行、新执行、应用、目标环境及旧执行是否已进入部署阶段。通知失败不改变旧执行结果。
_Avoid_: 失败通知、成功通知

**应用结果通知（Application Outcome Notification）**:
针对一个应用交付发送的最终结果通知。仓库分发 Job 中不为单应用发送开始通知，只发送应用结果通知。
_Avoid_: 分发汇总通知、应用开始通知

**分发汇总通知（Dispatch Summary Notification）**:
仓库分发 Job 完成后发送的总体通知，汇总成功、跳过、撤回、被拒绝和失败数量，并标出异常仓库。
_Avoid_: 单应用结果通知

**环境交付执行（Environment Delivery Execution）**:
环境交付 Job 的一次运行。同一个环境交付 Job 在任意时刻最多只有一个活动执行，不同目标环境的 Job 可以并行执行。
_Avoid_: 并发部署、跨环境执行

**执行取代（Execution Supersession）**:
同一环境交付 Job 的新执行取代尚未完成的旧执行。新执行必须等待旧执行退出并完成工作区清理尝试后才能开始；清理失败会使旧执行失败，但不阻断新执行。
_Avoid_: 并发执行、排队执行

**代码托管空间（Code Hosting Space）**:
由 Codeup 或 GitLab 服务管理、可通过统一 API 枚举其中仓库的范围。
_Avoid_: 单个仓库、Jenkins Job

**仓库交付配置（Repository Delivery Configuration）**:
以受限交付 Jenkinsfile 形式保存在源码仓库中，用于声明一个可交付单元应调用的共享库步骤及其环境交付参数。仓库分发 Job 递归搜索配置，一份匹配配置代表一个可交付单元。
_Avoid_: 共享库默认配置、Jenkins Job 配置、任意 Pipeline 脚本

**交付 Jenkinsfile 文件名（Delivery Jenkinsfile Name）**:
由一个仓库分发 Job 指定、用于递归发现仓库交付配置的单个精确文件名。该名称不支持通配符、正则表达式或同时配置多个名称；各模块目录中的同名文件分别代表可交付单元。
_Avoid_: 配置文件路径、文件名模式、文件名列表

**受限交付 Jenkinsfile（Restricted Delivery Jenkinsfile）**:
仓库分发 Job 可读取和执行的仓库交付声明。它只能包含受支持的交付配置并调用一个允许的共享库步骤；不具备运行任意 Jenkins Pipeline 逻辑的权限。
_Avoid_: 完整 Jenkins Pipeline、任意 Groovy 脚本

**仓库分发 Job（Repository Dispatch Job）**:
一种环境交付 Job；它扫描代码托管空间内的仓库，读取各仓库中属于自身目标环境的仓库交付配置，并在自身执行上下文中直接执行相应应用的交付。
_Avoid_: 应用交付 Job、下游 Job 触发器
