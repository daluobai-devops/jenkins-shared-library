# Fail delivery when workspace cleanup fails

Jenkins 工作区清理是应用交付的强制成功条件，而不是可忽略的附属动作。即使构建产物已部署并通过就绪验证，只要清理失败，交付结果仍为失败；清理失败不阻断取代它的新执行，但新执行开始时必须删除非声明残留，只保留显式构建缓存，初始化失败则新执行失败。该选择以避免残留污染后续构建为优先，接受“应用已更新但本次交付失败”的非直观状态。
