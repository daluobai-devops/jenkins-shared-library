# Preflight unique application identifiers before dispatch

仓库分发 Job 必须在执行任何应用交付前解析全部候选配置；每个可交付单元都必须显式提供应用标识，缺失时预检失败，不能从仓库名、目录名或 Job 名推导。应用标识必须在目标环境内唯一；若多个单元使用相同标识，所有冲突单元都被拒绝，其他无冲突单元继续交付，最终分发结果失败。该选择避免默认名称碰撞以及后续单元覆盖相同服务或目录。
