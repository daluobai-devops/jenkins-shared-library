# Separate delivery opt-in from environment authorization

仓库交付配置只表示可交付单元希望参与交付，不自动授予其向当前目标环境部署的权限；环境交付授权由仓库分发 Job 独立控制。授权粒度是源码仓库：获授权仓库在受控配置发现引用上的全部匹配单元继承授权，不再维护单元级白名单。只有当代码托管空间内所有仓库维护者都被信任可向该环境交付时，才允许显式授权全部仓库，否则必须使用仓库白名单。该边界防止任意未授权仓库仅通过新增配置文件就获得测试或生产环境的交付能力，同时接受获授权 monorepo 内模块共享仓库信任边界。
