# Separate configuration discovery from source selection

仓库分发 Job 通过自身固定的配置发现引用递归查找仓库交付配置，而每个可交付单元的源码引用只选择实际构建的代码版本，不能改变配置的发现位置。单元源码目录按相对路径在所选源码引用中解析；目录不存在时预检失败，不回退仓库根目录或猜测其他目录。该边界牺牲了任意分支自行声明交付及自动适配目录移动的灵活性，以换取确定的发现结果，并防止未经环境 Job 信任的业务分支自行进入环境交付。
