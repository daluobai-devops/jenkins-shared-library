# Resolve delivery configuration by layered precedence

应用交付按照“共享库默认配置 < 扩展配置 < 主交付配置 < Jenkins Job 参数”的固定优先级生成有效交付配置。旧配置必须先在读取或解析入口转换为当前统一结构，再参与合并；后续实现只消费有效交付配置。同一入口不使用配置版本分支，并长期兼容它曾接受的旧配置。该顺序既保留旧配置兼容性，也允许团队公共配置、应用配置和单次执行覆盖各自承担清晰职责。
