# Reject unimplemented operation modes

现有交付入口只执行正式支持的应用交付能力；旧配置未声明操作模式时默认执行 `DEPLOY`，但配置显式请求 `SERVICE`、`TEARDOWN` 或其他未实现模式时，必须立即拒绝并使任务失败，不能忽略该值后继续部署。该选择既保留旧配置兼容性，也避免明确的操作意图产生相反效果；规划能力只有在获得职责单一的正式入口后才能被配置调用。
