# Use new entries for breaking capability changes

共享库步骤不通过 `configVersion` 在同一入口中承载破坏性配置变化；每个既有入口长期兼容它曾接受的旧配置，并在入口统一转换为当前内部结构。无法兼容的重大能力变化通过新增 `vars` 入口建立独立契约。旧入口可以弃用并停止增加新能力，但只有完成使用方核对、迁移和明确公告后，才能通过独立决策删除。该选择避免同一入口内长期散落版本分支，代价是共享库需要同时维护旧入口和替代入口。
